vacío principal ()
- {int N, c, r, s = 0;
+ { 	int N, c, r, s = 0;
 	hacer
 	{cout << "Ingrese un numero mayor a tres digitos:" << endl;
 	 cin >> N;
 	} while (N <100);
- mientras (N> 0)
- {r = N% 10;
- s = s + r;
- c = N / 10;
- N = c;
-}
+ 	mientras (N> 0)
+ 	{r = N% 10;
+   	s = s + r;
+   	c = N / 10;
+   	N = c;
+ 	}
 cout << "La suma es:" << s;
 _getch ();
 }
